export interface GetImagesResponse {
    code: number;
    message: string;
    data: string[] | null;
}

export interface UploadImagesResponse {
    code: number;
    message: string;
    data: string[] | null;
}
